import React, { Component } from 'react';
import { Text } from 'react-native';

class Calling extends Component {
  render() {
    return (
      <Text> Calling Screen </Text>
    )
  }
}

export default Calling;
